#include <stdio.h>
#include <stdlib.h>

extern char **environ;

int main() {
    char **envp = environ;

    // Loop through the environment variables and print them out
    while (*envp != NULL) {
        printf("%s\n", *envp);
        envp++;
    }

    return 0;
}
